# TREKBASE HOLDING PAGE

## Install

  - Clone the repo
  - Create local DB with mongodb named trekbase
  - npm install
  - Ask for .env file

## Run

  ### Dev (two terminals need to be run at the same time):
  - npm run build:dev:watch
  - npm run dev

  ### Prod:
  - npm run build:prod